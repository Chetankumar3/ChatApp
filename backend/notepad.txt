- An API to mark message(s) seen. (one side push)
- When user come online (connection established), send all messages that are not received. Store all chats and only send new ones.

Temp:
- Group addition notification

Frontend:
- Why APIs called twice??
- Notification

Both:
- Forgot Password
- Change DP
 - Set mobile number (with OTP)